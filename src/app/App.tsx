import { BookStack } from "./components/BookStack";

export default function App() {
  return (
    <div className="size-full bg-gradient-to-br from-purple-50 to-blue-50 flex items-center justify-center">
      <BookStack />
    </div>
  );
}