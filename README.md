
  # Book Carousel Interface

  This is a code bundle for Book Carousel Interface. The original project is available at https://www.figma.com/design/Mwyjc41F2rhq5X6kMNtrNE/Book-Carousel-Interface.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  